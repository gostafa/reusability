---
version: "3.5"
vars:
  EXTRA_ARGS: ""
  GLOBAL_GO_BIN:
    sh: |
      gobin="${GOBIN:-}"
      if [ -z "$gobin" ] && command -v go >/dev/null 2>&1; then
        gobin="$(go env GOBIN)"
      fi
      if [ -z "$gobin" ]; then
        gopath="${GOPATH:-}"
        if [ -z "$gopath" ] && command -v go >/dev/null 2>&1; then
          gopath="$(go env GOPATH)"
        fi
        gobin="${gopath:-$HOME/go}/bin"
      fi
      printf '%s' "$gobin"
includes:
  go:
    taskfile: go/Taskfile.yml
    vars:
      INSTALL_DIR_UNIX: "/usr/local"
      GO_ROOT_UNIX: "{{.INSTALL_DIR_UNIX}}/go"
      GO_BIN_UNIX: "{{.GO_ROOT_UNIX}}/bin"
      GO_CMD_UNIX: "{{.GO_BIN_UNIX}}/go"
      GO_VERSION_URL: https://go.dev/VERSION?m=text
      GO_DOWNLOAD_BASE_URL: https://go.dev/dl
      GO_VERSION: ""
      GOLANGCI_LINT_VERSION: ""
      GOVULNCHECK_VERSION: ""
      GOSEC_VERSION: ""
      GO_FMT_SKIP_PATTERN: ""
      GO_LINT_SKIP_PATTERN: ""
      GO_JUNIT_REPORT: ""
      # Written next to the project's golangci-lint config so relative paths inside
      # the merged copy keep resolving. Overwritten on every run, never deleted.
      GO_INTERNAL_SKIP_CONFIG: ".golangci-taskotter-skip.yml"
      GO_COVER_PROFILE: ""
      GO_FUZZTIME: ""
      # Formatter selection shared by every golangci-lint fmt invocation (both the
      # skip-pattern and plain paths, on Unix and Windows). Kept in one place so the
      # four call sites cannot drift apart.
      GO_FMT_FORMATTER_FLAGS: >-
        -E gci -E gofmt -E gofumpt -E goimports -E golines -E swaggo
      GLOBAL_GO_BIN: '{{.GLOBAL_GO_BIN}}'
  staticcheck:
    taskfile: staticcheck/Taskfile.yml
    vars:
      STATICCHECK_LINT_SKIP_PATTERN: ""
      STATICCHECK_VERSION: "2026.1"
      STATICCHECK_RELEASE_BASE_URL: https://github.com/dominikh/go-tools/releases/download
      GLOBAL_GO_BIN: '{{.GLOBAL_GO_BIN}}'
  actionlint:
    taskfile: actionlint/Taskfile.yml
    vars:
      ACTIONLINT_LINT_SKIP_PATTERN: ""
      ACTIONLINT_VERSION: "1.7.12"
      ACTIONLINT_EXTRA_ARGS: ""
      ACTIONLINT_TARGETS: ""
  yamllint:
    taskfile: yamllint/Taskfile.yml
    vars:
      YAMLLINT_LINT_SKIP_PATTERN: ""
      UV_LOAD: 'export PATH="$HOME/.local/bin:$PATH"'
      TARGETS: "."
      CONFIG: ""
      EXTRA_ARGS: '{{.EXTRA_ARGS}}'
      YAMLLINT_VERSION: ""
      YAMLFIX_VERSION: ""
  zizmor:
    taskfile: zizmor/Taskfile.yml
    vars:
      ZIZMOR_LINT_SKIP_PATTERN: ""
      ZIZMOR_EXTRA_ARGS: "--offline"
      ZIZMOR_TARGETS: ".github"
      ZIZMOR_VERSION: "1.25.2"
  adrs:
    taskfile: adrs/Taskfile.yml
    vars:
      CARGO_BIN_UNIX: "$HOME/.cargo/bin"
      EXTRA_ARGS: '{{.EXTRA_ARGS}}'
      ADRS_VERSION: ""
tasks:
  install:
    desc: Run install for synced TaskOtter modules
    cmds:
      - task: go:install
      - task: staticcheck:install
      - task: actionlint:install
      - task: yamllint:install
      - task: zizmor:install
      - task: adrs:install
  install:undo:
    desc: Run install:undo for synced TaskOtter modules
    cmds:
      - task: go:install:undo
      - task: staticcheck:install:undo
      - task: actionlint:install:undo
      - task: yamllint:install:undo
      - task: zizmor:install:undo
      - task: adrs:install:undo
  lint:
    desc: Run lint for synced TaskOtter modules
    cmds:
      - task: go:lint
      - task: staticcheck:lint
      - task: actionlint:lint
      - task: yamllint:lint
      - task: zizmor:lint
  lint:fix:
    desc: Run lint:fix for synced TaskOtter modules
    cmds:
      - task: go:lint:fix
      - task: yamllint:lint:fix
  upgrade:
    desc: Run upgrade for synced TaskOtter modules
    cmds:
      - task: go:upgrade
      - task: staticcheck:upgrade
      - task: actionlint:upgrade
      - task: yamllint:upgrade
      - task: zizmor:upgrade
      - task: adrs:upgrade
  version:
    desc: Run version for synced TaskOtter modules
    cmds:
      - task: go:version
      - task: staticcheck:version
      - task: actionlint:version
      - task: yamllint:version
      - task: zizmor:version
      - task: adrs:version
