// Package version exposes the modularity tool version string.
//
// Release builds may override Version at link time with -X; the CLI and report
// ToolInfo both read from this package.
package version
