// Package application coordinates the cohesion metric feature.
//
// It consumes per-type method and field facts, builds method-field and
// method-field matrices once per type, and produces LCOM and TCC results.
package application
